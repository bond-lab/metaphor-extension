[project]
name = "metaphor-extension"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.3",
    "pytest>=9.0.3",
    "wn>=1.1",
]

[tool.uv]
package = true

[tool.setuptools]
packages = ["metaphor_extension"]

[dependency-groups]
dev = [
    "setuptools>=82.0.1",
]
